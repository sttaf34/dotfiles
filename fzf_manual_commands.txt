source ~/.bashrc
nodenv rehash
ifconfig | grep 192.168
history | tail -10
npx gatsby develop
npx gatsby develop -H 0.0.0.0
git add . && git cmm
grm -rf ~/.Trash/*
serve ~/Projects/note/htmlcss
echo $PATH | tr ':' '\n'
nodenv versions
docker image ls
docker container ls
docker container ls -a
git commit --amend
npm ls --depth=0
npm ls -g --depth=0
docker container ls -aq | xargs docker container rm
docker image ls -aq | xargs docker image rm
docker-compose up
docker-compose down
docker-compose build --no-cache
heroku login
heroku container:login
yarn add --dev
yarn install
docker container exec -it CONTAINER_ID /bin/bash
git-echo-repository-status
docker-compose exec application bash
docker-compose exec db bash
circleci config validate
docker network ls
docker network inspect NETWORK_ID
git-echo-hooks
osascript -e 'tell application "Finder" to sleep'
docker-compose config
curl ifconfig.io
lsof -n -P -iTCP -sTCP:LISTEN
ps aux | head -1 && ps aux | grep ssh
git remote -v
